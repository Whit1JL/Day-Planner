// jquery 

// user when loading page, current date appear at top of screen
// user scroll down, see color coded time blocks - past(gray), present(red), future(green)
// user click into time block and save event 
// user clicks save button, data input is saved into a local storage
// when page refreshed the calander is updated

// use moment js for math calculation for determining time



$(document).ready(function () {
    // all of the code is going inside brackets
    console.log("document ready")

    
})